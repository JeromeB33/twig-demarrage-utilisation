{% extends 'layout.html.twig' %}

{% block content %}
    <h1>Products</h1>

    {% for product in products|slice(0 , 3) %}
        {{ product }}, 
    {% endfor %}
<br>
    {% for product in products|slice(3 , 3) %}
        {{ product }},
    {% endfor %}


{{ dump(products) }}
{% endblock %}
